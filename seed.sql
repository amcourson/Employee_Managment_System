DROP DATABASE IF EXISTS seed_db;
CREATE DATABASE seed_db;

USE DATABASE seed_db;

CREATE TABLE seed_db;
    department VARCHAR(30) NOT NULL;
    employee_role VARCHAR(30) NOT NULL;
    employee_name VARCHAR(30) NOT NULL;
    PRIMARY KEY (id);



